const routes = require("express")();

//registers our authentication routes with Express.
//manage role https://github.com/devgson/soshace-rbac
//routes.use("/users", require("./controllers/users"));
//TODO add a routine to download the json from atlassian and to save in database.
module.exports = routes;
